# 机械臂任务验证

本项目区分预设演示与实际物理仿真，帮助使用者运行任务、复查证据并比较受控参数。

## Language

**任务**：要由机器人完成并按明确条件判断的作业，本阶段为单方块抓取并放置。
_Avoid_: 将任务等同于一次运行或一个页面。

**运行**：在一组固定配置和初始条件下执行一次任务的尝试；无论成功、任务失败或执行异常，都应保留可追溯身份。
_Avoid_: 将运行结束等同于任务成功。

**参数方案**：同一控制逻辑下的一组参数取值。方案名称用于识别，实际参数与控制器版本共同决定是否相同。
_Avoid_: 将“基线”或“偏移”名称当作成功或失败的保证。

**任务结果**：根据运行证据和评估条件得出的成功或失败结论；执行异常导致没有有效报告时，任务结果未知。
_Avoid_: 把进程错误当成已证实的抓取失败。

**重复性检查**：相同模型、初始条件、评估口径和参数方案下的多次运行检查，不代表多场景覆盖或两种策略比较。
_Avoid_: 用重复次数自动推导泛化能力。

**受控参数比较**：其他实验条件一致，仅指定参数变化时对不同参数方案的运行结果进行比较。
_Avoid_: 把同名但不同参数的运行合为同一方案，或把不同模型混算。

**运行证据**：用于复查一次运行的配置、状态、轨迹、事件、模型身份和判定依据；缺失或损坏必须明确，不由展示画面补造。
_Avoid_: 将能读取报告等同于全部证据完整。

**历史演示**：由预设场景产生的 V1 展示数据，不属于实际物理运行的统计样本。旧平面物理任务也与 Panda 分别解释。
_Avoid_: 把页面名字或版本标签当作统一数据来源。
